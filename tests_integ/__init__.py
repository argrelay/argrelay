# TODO: split integration and unit tests:
#       * rename this package  `integ_tests`
#       * move this dir under `tests` (move tests under `tests` as `unit_tests`)
#       * make sure build runs only `unit_tests`
#       * move integration tests here
