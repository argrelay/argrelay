# Sample config for `argrelay` client.
# To test, symlink this files as user dot file - see `dev-shell.bash` script.

use_local_requests: false

connection_config:
    server_host_name: localhost
    server_port_number: 8787
